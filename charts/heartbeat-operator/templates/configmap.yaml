apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "heartbeat-operator.fullname" . }}-config
  labels:
    {{- include "heartbeat-operator.labels" . | nindent 4 }}
data:
  gates.json: |
    {{- .Values.probes | toJson | nindent 4 }}